# GitHub Token - 从 /home/admin/.token 文件读取
# 此文件仅用于文档说明，实际 Token 存储在 /home/admin/.token

# 使用方式:
# TOKEN = read_token_from_file("/home/admin/.token")
# 或
# TOKEN = os.environ.get("GITHUB_TOKEN")

# 注意: 不要在代码中硬编码 Token!